import React from 'react'

/* scroll component */
const Scroll = props => (
    <img className = {props.className} src="./images/scroll_arrow.svg" alt="arrow down to scroll" aria-label="Aria label for arrow down to scroll"/>
)

export default Scroll;